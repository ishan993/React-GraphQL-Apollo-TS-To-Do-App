import { GraphQLLanguageServiceAdapter } from '../graphql-language-service-adapter';
export declare class AdapterFixture {
    adapter: GraphQLLanguageServiceAdapter;
    private _source;
    constructor(scriptFileName: string, schemaJson?: {
        data: any;
    });
    source: string;
}
