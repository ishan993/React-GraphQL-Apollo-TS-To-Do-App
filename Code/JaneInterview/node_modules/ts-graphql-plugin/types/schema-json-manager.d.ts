import * as ts from 'typescript/lib/tsserverlibrary';
export declare class SchamaJsonManager {
    private _info;
    private _schemaPath;
    private _watcher;
    private _onChanges;
    constructor(_info: ts.server.PluginCreateInfo);
    getSchema(): any;
    getAbsoluteSchemaPath(projectRootPath: string, schemaPath: string): string;
    registerOnChange(cb: (schema: any) => void): () => void;
    startWatch(interval?: number): void;
    closeWatch(): void;
    private _log(msg);
}
