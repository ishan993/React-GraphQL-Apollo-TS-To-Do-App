type Item {
  id: String!
  name: String!
}

type Query {
  items: [Item]
  item(id: String!): Item
}

type Mutation {
  addItem (
    name: String!
  ): Item!

  deleteItem (
    id: String!
  ): Item!
}

schema {
  query: Query
  mutation: Mutation
}